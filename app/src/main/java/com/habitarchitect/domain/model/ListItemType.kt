package com.habitarchitect.domain.model

/**
 * Type of list item - resistance (reasons NOT to do bad habit)
 * or attraction (reasons TO do good habit).
 */
enum class ListItemType {
    RESISTANCE,
    ATTRACTION
}
